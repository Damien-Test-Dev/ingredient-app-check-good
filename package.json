{
  "name": "ingredient-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate:index": "node scripts/generate-index.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "prebuild": "npm run generate:index && npm run validate:data",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
